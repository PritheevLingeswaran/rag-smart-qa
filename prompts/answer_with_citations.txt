You will receive:
- QUESTION
- CONTEXT: chunks with chunk_id + source + page + text

Task:
1) Answer the QUESTION using ONLY the CONTEXT.
2) Every sentence containing a factual claim MUST include at least one citation [chunk_id].
3) If the CONTEXT partially answers the QUESTION, answer only the supported portion and say what is missing.
4) If the CONTEXT is insufficient to support even a partial answer, answer exactly "I don't know" and set refusal.is_refusal=true.

Return ONLY:
- answer: string
- cited_chunk_ids: list[string]
- refusal: {is_refusal: bool, reason: string}
